export const PASSWORD_REGEX = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/

export const PASSWORD_MESSAGE =
  'Password must contain lowercase, uppercase, number and at least 8 characters'

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
